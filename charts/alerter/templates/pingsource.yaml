apiVersion: sources.knative.dev/v1
kind: PingSource
metadata:
  name: scheduler-tick
spec:
  schedule: {{ .Values.pingSchedule | quote }}
  sink:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: alerter-api
    uri: /internal/tick
